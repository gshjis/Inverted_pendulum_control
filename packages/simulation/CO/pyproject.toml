[tool.poetry]
name = "co"
version = "0.1.0"
description = "Control object (inverted pendulum) math simulation"
authors = ["gshjis <ilya.togan@gmail.com>"]
readme = "README.md"

packages = [
    { include = "*", from = "." }
]


[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"